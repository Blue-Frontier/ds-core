# 变更说明

<!-- 

# 与兄弟仓

- 是否改了内核？若是，是否已在 Blue-Frontier/ds-core 提交并打 tag？
- 是否需要 bump `vendor/ds-core` submodule？目标 tag:
- 三仓版本号是否对齐？

# 检查

- [ ] `pnpm install` / `pnpm test` / `pnpm lint`
- [ ] 文档 URL 使用绝对路径
- [ ] 未把 monorepo 专有路径写进用户文档

-->

<!-- ssot: Blue-Frontier/.github -->
